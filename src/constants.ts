export const PADDLE_WIDTH = 0.5;
export const PADDLE_HEIGHT = 3;
export const PADDLE_DEPTH = 1;
export const PADDLE_OFFSET_X = 8;

export const FIELD_WIDTH = 16;
export const FIELD_HEIGHT = 10;
export const WALL_THICKNESS = 1;

export const BALL_DIAMETER = 1;
export const INITIAL_BALL_SPEED = 0.08;
export const BALL_SPEED_INCREMENT = 1.05;

export const PADDLE_MOVE_STEP = 0.3;
export const SCORE_BOUNDARY_X = PADDLE_OFFSET_X + 1;

export const DEFAULTS = {
  theme: 'neon',
  speedMode: 'normal',
};